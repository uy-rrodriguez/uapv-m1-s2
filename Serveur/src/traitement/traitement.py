import sys, traceback, time
import AppMP3Player

class Traitement:
    def __init__(self):
        pass

    def traiterCommande(self, ipClient, commande):
        print "Traitement->traiterCommande"
        return commande #Commande
